<?php if ( ! defined( 'FW' ) ) {
	die( 'Forbidden' );
}

$manifest = array();

$manifest['id'] = 'bubulla';

$manifest['supported_extensions'] = array(
	'backups' => array(),
	'megamenu' => array(),
);
